<form method="post" action="{{ path('beneficiaire_delete', {'id': beneficiaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beneficiaire.id) }}">
    <button style="border-radius: 10px; padding : 5px; margin-bottom : 10px;" class="btn btn-info" >Delete</button>
</form>
