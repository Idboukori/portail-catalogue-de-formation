{% extends 'base.html.twig' %}

{% block title %}Formation index{% endblock %}
{% block sty %}

{% endblock %}
{% block body %}
    <!-- <div class="row">
    <h1 class="col-md-10">Formation list</h1>
    {% if app.user and is_granted('ROLE_ADMIN') %}
        <div class="col-md-2">
    <a  style="margin-top: 20px;" class="btn btn-info " href="{{ path('formation_new') }}">Create new</a>
        </div>
        {% endif %}
    </div> -->
    {% block sear %}
    <div class="col-md-3" style="margin-top: 15px; margin-bottom: 10px;">
    {{form_start(form)}}
    {{form_row(form.search)}}

    {{form_end(form)}}
    </div>
    {% endblock %}
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Intitule</th>
                <th>Description</th>
                <th>Duree</th>
                <th>Type</th>
                {# <th>actions</th> #}
            </tr>
        </thead>
        <tbody>
        {% for formation in formations %}
            <tr>
                <td><a href="{{ path('formation_show', {'id': formation.id}) }}">{{ formation.intitule }}</a></td>
                <td>{{ formation.description }}</td>
                <td>{{ formation.duree }}h</td>
                <td>{{ formation.type }}</td>
                {# <td>
                    <a style="border-radius: 10px; padding : 5px; margin-bottom : 10px;" class="btn btn-info"  href="{{ path('formation_show', {'id': formation.id}) }}">show</a>
                    {% if app.user %}
                        {% if  is_granted('ROLE_ADMIN') and formation.formateur.email == app.user.email %}
                        <a style="border-radius: 10px; padding : 5px; margin-bottom : 10px;" class="btn btn-info"  href="{{ path('formation_edit', {'id': formation.id}) }}">edit</a>
                        <a style="border-radius: 10px; padding : 5px; margin-bottom : 10px;" class="btn btn-info"  href="{{ path('chapitre_new', {'id': formation.id}) }}">NewChapter</a>
                        
                        {% else %}
                            {% if formation.formateur.email == app.user.email %}
                                <p> Your formation</p>
                            {% else %}
                            {% if list is empty %}
                            <a style="border-radius: 10px; padding : 5px; margin-bottom : 10px;" class="btn btn-info"  href="{{ path('formation_inscrire', {'id': formation.id , 'id2': app.user.email}) }}">S'inscrire</a>
                            {% else %}
                            
                                {% if formation in list %}
                                
                                    <p> Déja inscrit</p>

                                {% else %}
                                    <a style="border-radius: 10px; padding : 5px; margin-bottom : 10px;" class="btn btn-info"  href="{{ path('formation_inscrire', {'id': formation.id , 'id2': app.user.email}) }}">Inscription</a>

                                {% endif %}

                            {% endif %}
                        {% endif %}
                        {% endif %}
                    {% endif %}

                </td> #}
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div style="margin-bottom: 10px;" class="Page navigation example">
            {{ knp_pagination_render(formations) }}
        </div>

    
{% endblock %}
