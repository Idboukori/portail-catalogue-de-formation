<form method="post" action="{{ path('demande_formateur_delete', {'id': demande_formateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_formateur.id) }}">
    <button class="btn">Delete</button>
</form>
