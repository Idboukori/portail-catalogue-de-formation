{% extends 'base.html.twig' %}

{% block title %}Qa{% endblock %}

{% block body %}
    <h1>Qa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qa.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ qa.question }}</td>
            </tr>
            <tr>
                <th>Answer</th>
                <td>{{ qa.answer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('qa_index') }}">back to list</a>

    <a href="{{ path('qa_edit', {'id': qa.id}) }}">edit</a>

    {{ include('qa/_delete_form.html.twig') }}
{% endblock %}
